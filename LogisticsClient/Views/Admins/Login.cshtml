@model LogisticsClient.Models.Admin
@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_Layout3.cshtml";
}
<style>
    body {
        margin: 0;
        padding: 0;
        font-family: sans-serif;
        background: #e6e6e6;
    }

    .box {
        width: 300px;
        padding: 40px;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%,-50%);
        background: #ffffff;
        text-align: center;
        border-radius: 30px;
        -webkit-box-shadow: 0px 10px 40px -10px rgba(0, 0, 0, 0.7);
        -moz-box-shadow: 0px 10px 40px -10px rgba(0, 0, 0, 0.7);
        box-shadow: 0px 10px 40px -10px rgba(0, 0, 0, 0.7);
    }

        .box input[type="text"], .box input[type="password"] {
            border: 0;
            background: none;
            display: block;
            margin: 20px auto;
            text-align: center;
            border: 2px solid #ff1a8c;
            padding: 10px 10px;
            width: 150px;
            outline: none;
            border-radius: 24px;
            transition: 0.25s;
        }

        .box h1 {
            color: #ff1a8c;
            text-transform: uppercase;
            font-weight: 700;
        }

        .box input[type="text"]:focus, .box input[type="password"]:focus {
            width: 190px;
            border-color: #66ccff;
        }

        .box input[type="submit"] {
            border: 0;
            background: none;
            display: block;
            margin: 2px auto;
            text-align: center;
            border: 2px solid #66ccff;
            padding: 10px 40px;
            outline: none;
            border-radius: 24px;
            transition: 00.25s;
            cursor: pointer;
        }

            .box input[type="submit"]:hover {
                background: #66ccff;
            }
</style>
@*<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    </div>*@

@*<div class="row">
    <div class="col-md-4">
    <form asp-action="Login" asp-controller="Admins">
    <div class="form-group">
    <label asp-for="Username" class="control-label">Username</label>
    <input asp-for="Username" class="form-control" />
    <span asp-validation-for="Username" class="text-danger"></span>
    </div>
    <div class="form-group">
    <label asp-for="Password" class="control-label">Password</label>
    <input asp-for="Password" class="form-control" />
    <span asp-validation-for="Password" class="text-danger"></span>
    </div>
    <div class="form-group">
    <input type="submit" value="login" class="btn btn-primary" />
    </div>
    <div asp-validation-summary="ModelOnly" class="text-danger">
    </div>
    <label>@Html.Raw(ViewBag.Message)</label>
    <label>@Html.Raw(ViewBag.User)</label>
    </form>
    </div>
    </div>*@


<form class="box" asp-action="Login" asp-controller="Admins">
    <h1>LOG IN</h1>
    <input type="text" placeholder="Username" asp-for="Username">
    <input type="password" placeholder="Password" asp-for="Password">
    <input type="submit" value="LOGIN">
</form>